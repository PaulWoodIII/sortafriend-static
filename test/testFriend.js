var Friend = require('./../js/models/friend');
var expect = require('chai').expect
var singleUser = require('./fixtures/oneUserResult');

describe('Friend',function(){
    if('can be initalized with JSON from the webservice',function(){
        
    });
    
    //id attributes
});